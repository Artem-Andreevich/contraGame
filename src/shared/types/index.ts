export type TArea = {
  x: number;
  y: number;
  width: number;
  height: number;
};

export type TPrevPoint = {
  x: number;
  y: number;
};

export type TPressedArrowContext = {
  pressedArrowLeft: boolean;
  pressedArrowRight: boolean;
  pressedArrowUp: boolean;
  pressedArrowDown: boolean;
};
